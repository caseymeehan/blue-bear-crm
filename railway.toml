[build]
builder = "NIXPACKS"

[build.nixpacks]
version = "8.2"

[deploy]
startCommand = "php database/run_migrations.php && php -S 0.0.0.0:${PORT:-8080}"

[[volumes]]
name = "saas-data"
mountPath = "/app/database"

