[phases.setup]
nixPkgs = ["php82", "php82Packages.composer"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-interaction"]

[start]
cmd = "php database/run_migrations.php && php -S 0.0.0.0:${PORT:-8080}"

